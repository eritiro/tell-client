- dar estilos copados (chusmear bootstrap)
- hacer pantallita de login (login/register)
- resolver el problema de login fallido. no se llama al callback de error
- hacer repositorio para datos de usuario
- agregar logout
- hacer llamada de codigo qr al server
- pantalla de resultados
- pantalla para comentar
- ver el click en el back button del device
- que el server devuelva la url completa de las imágenes
- deshardcodear config
- hacer un modo sin conexión.
- la redirección a login no deja salir de la app (home -> login (back button) -> home)
